import { createTheme } from "./stitches.config";

export const darkTheme = createTheme("dark-theme", {
  colors: {
    primary: "#3291ff",
    secondary: "#a855f7",
    background: "#111111",
    text: "#ededed",
    muted: "#999999",
  },
});
